local _TweenService = game:GetService('TweenService')

game:GetService('RunService')

local _PlayerGui = game:GetService('Players').LocalPlayer:WaitForChild('PlayerGui')

if _PlayerGui:FindFirstChild('KibaCyberIntro') then
    _PlayerGui.KibaCyberIntro:Destroy()
end

local _ScreenGui = Instance.new('ScreenGui')

_ScreenGui.Name = 'KibaCyberIntro'
_ScreenGui.ResetOnSpawn = false
_ScreenGui.IgnoreGuiInset = true
_ScreenGui.Parent = _PlayerGui

local _Sound = Instance.new('Sound')

_Sound.SoundId = 'rbxassetid://8622488090'
_Sound.Volume = 10
_Sound.Looped = false
_Sound.Parent = _ScreenGui

local _Sound2 = Instance.new('Sound')

_Sound2.SoundId = 'rbxassetid://452267918'
_Sound2.Volume = 10
_Sound2.Looped = false
_Sound2.Parent = _ScreenGui

local _Frame = Instance.new('Frame')

_Frame.Size = UDim2.new(1, 0, 1, 0)
_Frame.Position = UDim2.new(0, 0, 0, 0)
_Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
_Frame.BorderSizePixel = 0
_Frame.Parent = _ScreenGui

local _Frame2 = Instance.new('Frame')

_Frame2.Size = UDim2.new(1, 0, 1, 0)
_Frame2.BackgroundTransparency = 1
_Frame2.Parent = _ScreenGui

local _Frame3 = Instance.new('Frame')

_Frame3.Size = UDim2.new(0.8, 0, 0.35, 0)
_Frame3.BackgroundTransparency = 1
_Frame3.AnchorPoint = Vector2.new(0.5, 0.5)
_Frame3.Position = UDim2.new(0.5, 0, 0.45, 0)
_Frame3.Parent = _Frame2

local function v13(_, p9, p10, p11)
    local _Frame4 = Instance.new('Frame')

    _Frame4.AnchorPoint = Vector2.new(0.5, 0.5)
    _Frame4.Position = UDim2.new(0.5, 0, 0.5, 0)
    _Frame4.Size = UDim2.new(0.9 * p11, 0, 0.35 * p11, 0)
    _Frame4.BackgroundColor3 = p9
    _Frame4.BackgroundTransparency = p10
    _Frame4.BorderSizePixel = 0
    _Frame4.Parent = _Frame3

    return _Frame4
end

v13(1, Color3.fromRGB(120, 0, 160), 0.85, 1.2)
v13(1, Color3.fromRGB(0, 200, 230), 0.92, 1.05)
v13(1, Color3.fromRGB(160, 40, 200), 0.95, 0.95)

local _TextLabel = Instance.new('TextLabel')

_TextLabel.BackgroundTransparency = 1
_TextLabel.Size = UDim2.new(0.9, 0, 0.4, 0)
_TextLabel.Font = Enum.Font.Code
_TextLabel.TextScaled = true
_TextLabel.Text = ''
_TextLabel.TextColor3 = Color3.fromRGB(200, 200, 255)
_TextLabel.TextStrokeTransparency = 0.6
_TextLabel.TextTransparency = 1
_TextLabel.Parent = _Frame3;

(function(p15)
    p15.AnchorPoint = Vector2.new(0.5, 0.5)
    p15.Position = UDim2.new(0.5, 0, 0.5, 0)
end)(_TextLabel)

_TextLabel.Position = UDim2.new(0.5, 0, 0.5, 0)

local _TextLabel2 = Instance.new('TextLabel')

_TextLabel2.BackgroundTransparency = 1
_TextLabel2.Size = UDim2.new(0.8, 0, 0.08, 0)
_TextLabel2.Font = Enum.Font.Code
_TextLabel2.TextScaled = true
_TextLabel2.Text = ''
_TextLabel2.TextColor3 = Color3.fromRGB(255, 120, 255)
_TextLabel2.TextTransparency = 1
_TextLabel2.Parent = _Frame2
_TextLabel2.AnchorPoint = Vector2.new(0.5, 0)
_TextLabel2.Position = UDim2.new(0.5, 0, 0.65, 0)

local _Frame5 = Instance.new('Frame')

_Frame5.Size = UDim2.new(0.6, 0, 0.03, 0)
_Frame5.AnchorPoint = Vector2.new(0.5, 0)
_Frame5.Position = UDim2.new(0.5, 0, 0.78, 0)
_Frame5.BackgroundColor3 = Color3.fromRGB(30, 0, 40)
_Frame5.BorderSizePixel = 0
_Frame5.Parent = _Frame2

local _Frame6 = Instance.new('Frame')

_Frame6.Size = UDim2.new(0, 0, 1, 0)
_Frame6.BackgroundColor3 = Color3.fromRGB(200, 30, 200)
_Frame6.BorderSizePixel = 0
_Frame6.Parent = _Frame5

local _Frame7 = Instance.new('Frame')

_Frame7.Size = UDim2.new(1, 0, 0.005, 0)
_Frame7.Position = UDim2.new(0, 0, 0.25, 0)
_Frame7.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
_Frame7.BackgroundTransparency = 0.93
_Frame7.BorderSizePixel = 0
_Frame7.ZIndex = 10
_Frame7.Parent = _Frame2

for v20 = 1, 8 do
    local v21 = _Frame7:Clone()

    v21.Position = UDim2.new(0, 0, 0.2 + v20 * 0.07, 0)
    v21.BackgroundTransparency = 0.94 + math.random() * 0.02
    v21.Parent = _Frame2
end

local _Frame8 = Instance.new('Frame')

_Frame8.Size = UDim2.new(1.2, 0, 0.5, 0)
_Frame8.AnchorPoint = Vector2.new(0.5, 0.5)
_Frame8.Position = UDim2.new(0.5, 0, 0.45, 0)
_Frame8.BackgroundColor3 = Color3.fromRGB(160, 40, 200)
_Frame8.BackgroundTransparency = 1
_Frame8.BorderSizePixel = 0
_Frame8.ZIndex = 9
_Frame8.Parent = _Frame2

local _Frame9 = Instance.new('Frame')

_Frame9.Size = UDim2.new(1, 0, 1, 0)
_Frame9.BackgroundTransparency = 1
_Frame9.BorderSizePixel = 0
_Frame9.ZIndex = 12
_Frame9.Parent = _Frame2

local function u26()
    local _Frame10 = Instance.new('Frame')

    _Frame10.Size = UDim2.new(1, 0, 1, 0)
    _Frame10.BackgroundTransparency = 1
    _Frame10.ZIndex = 2
    _Frame10.Parent = _Frame2

    for _ = 1, 80 do
        local _TextLabel3 = Instance.new('TextLabel')

        _TextLabel3.Size = UDim2.new(0, math.random(8, 14), 0, math.random(8, 14))
        _TextLabel3.Position = UDim2.new(math.random(), 0, math.random(), 0)
        _TextLabel3.BackgroundTransparency = 1
        _TextLabel3.Text = string.char(math.random(33, 126))
        _TextLabel3.TextColor3 = Color3.fromRGB(0, 200, 230)
        _TextLabel3.TextTransparency = 1
        _TextLabel3.Font = Enum.Font.Code
        _TextLabel3.TextScaled = true
        _TextLabel3.Parent = _Frame10

        delay(math.random() * 1.6, function()
            _TweenService:Create(_TextLabel3, TweenInfo.new(0.6), {TextTransparency = 0.7}):Play()
            wait(0.8 + math.random() * 1)
            _TweenService:Create(_TextLabel3, TweenInfo.new(0.8), {TextTransparency = 1}):Play()
        end)
    end
end

local u27 = false

local function u35(p28, p29, p30)
    if not u27 then
        u27 = true

        local v31 = tick()
        local _Position = _Frame2.Position

        while tick() - v31 < p28 do
            local v33 = (math.random() - 0.5) * 2 * p29
            local v34 = (math.random() - 0.5) * 2 * p29

            _Frame2.Position = UDim2.new(0, v33, 0, v34)

            wait(1 / p30)
        end

        _Frame2.Position = _Position
        u27 = false
    end
end
local function u44()
    local u36 = {}

    for v37 = 1, 6 do
        local _Frame11 = Instance.new('Frame')

        _Frame11.Size = UDim2.new(math.random(25, 75) / 100, 0, math.random(4, 12) / 100, 0)
        _Frame11.Position = UDim2.new(math.random(), 0, 0.35 + math.random() * 0.3, 0)
        _Frame11.BackgroundColor3 = v37 % 2 == 0 and Color3.fromRGB(160, 40, 200) or Color3.fromRGB(0, 200, 230)
        _Frame11.BorderSizePixel = 0
        _Frame11.BackgroundTransparency = 0.85
        _Frame11.ZIndex = 15
        _Frame11.Parent = _Frame9

        table.insert(u36, _Frame11)

        local v39 = math.clamp(_Frame11.Position.X.Scale + (math.random() - 0.5) * 0.15, 0, 1)

        _TweenService:Create(_Frame11, TweenInfo.new(0.08 + math.random() * 0.12, Enum.EasingStyle.Linear), {
            Position = UDim2.new(v39, 0, _Frame11.Position.Y.Scale, 0),
        }):Play()
    end

    delay(0.12, function()
        local v40, v41, v42 = ipairs(u36)

        while true do
            local v43

            v42, v43 = v40(v41, v42)

            if v42 == nil then
                break
            end
            if v43 and v43.Parent then
                v43:Destroy()
            end
        end
    end)
end
local function u50(p45, p46, p47)
    p45.Text = ''
    p45.TextTransparency = 0

    for v48 = 1, #p46 do
        local v49 = p46:sub(v48, v48)

        p45.Text = string.sub(p46, 1, v48)

        if v49 ~= ' ' then
            pcall(function()
                _Sound2:Play()
            end)
        end

        wait(p47 + math.random() * 0.01)
    end
end
local function u54()
    local _TextLabel4 = Instance.new('TextLabel')

    _TextLabel4.Size = UDim2.new(3, 0, 3, 0)
    _TextLabel4.BackgroundTransparency = 1
    _TextLabel4.Text = 'X'
    _TextLabel4.Font = Enum.Font.GothamBlack
    _TextLabel4.TextScaled = true
    _TextLabel4.TextColor3 = Color3.fromRGB(255, 60, 60)
    _TextLabel4.TextStrokeTransparency = 0.6
    _TextLabel4.AnchorPoint = Vector2.new(0.5, 0.5)
    _TextLabel4.Position = UDim2.new(0.5, 0, 0.5, 0)
    _TextLabel4.ZIndex = 20
    _TextLabel4.Parent = _ScreenGui
    _TextLabel4.TextTransparency = 1

    local v52 = _TweenService:Create(_TextLabel4, TweenInfo.new(0.14, Enum.EasingStyle.Quad), {TextTransparency = 0})

    v52:Play()
    v52.Completed:Wait()

    local v53 = _TweenService:Create(_TextLabel4, TweenInfo.new(1.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        Size = UDim2.new(0.4, 0, 0.4, 0),
        Rotation = 720,
        TextTransparency = 1,
    })

    v53:Play()
    _TweenService:Create(_Frame8, TweenInfo.new(0.45, Enum.EasingStyle.Quad), {BackgroundTransparency = 0.6}):Play()
    delay(0.25, function()
        u44()
        u35(0.4, 8, 40)
    end)
    v53.Completed:Wait()

    if _TextLabel4 and _TextLabel4.Parent then
        _TextLabel4:Destroy()
    end

    _TweenService:Create(_Frame8, TweenInfo.new(0.6), {BackgroundTransparency = 1}):Play()
end
local function u62(p55)
    local _Frame12 = Instance.new('Frame')

    _Frame12.Size = UDim2.new(1, 0, 1, 0)
    _Frame12.Position = UDim2.new(0, 0, 0, 0)
    _Frame12.BackgroundColor3 = Color3.new(1, 1, 1)
    _Frame12.BackgroundTransparency = 1
    _Frame12.ZIndex = 50
    _Frame12.Parent = _ScreenGui

    _TweenService:Create(_Frame12, TweenInfo.new(0.12), {BackgroundTransparency = 0}):Play()
    wait(0.12)
    _TweenService:Create(_Frame12, TweenInfo.new(0.35), {BackgroundTransparency = 1}):Play()

    local v57 = _ScreenGui
    local v58, v59, v60 = ipairs(v57:GetDescendants())

    while true do
        local u61

        v60, u61 = v58(v59, v60)

        if v60 == nil then
            break
        end
        if u61:IsA('TextLabel') or u61:IsA('TextButton') then
            pcall(function()
                _TweenService:Create(u61, TweenInfo.new(p55 or 0.8), {TextTransparency = 1}):Play()
            end)
        elseif u61:IsA('Frame') then
            pcall(function()
                _TweenService:Create(u61, TweenInfo.new(p55 or 0.8), {BackgroundTransparency = 1}):Play()
            end)
        end
    end

    wait((p55 or 0.8) + 0.05)

    if _ScreenGui and _ScreenGui.Parent then
        _ScreenGui:Destroy()
    end
end

spawn(function()
    local v68, v69 = pcall(function()
        wait(0.08)
        u54()
        pcall(function()
            _Sound:Play()
        end)
        u26()

        for _ = 1, 3 do
            u44()
            wait(0.09 + math.random() * 0.06)
        end

        u50(_TextLabel, 'K I B A   V4', 0.06)

        for v63 = 1, 3 do
            _TextLabel.TextColor3 = v63 % 2 == 0 and Color3.fromRGB(160, 40, 200) or Color3.fromRGB(0, 200, 230)

            u44()
            u35(0.08, 4, 50)
            wait(0.06)
        end

        _TextLabel2.Text = 'Initializing RNX Kiba V4 core...'

        _TweenService:Create(_TextLabel2, TweenInfo.new(0.6), {TextTransparency = 0}):Play()

        local v64 = 80

        for v65 = 1, v64 do
            _Frame6.Size = UDim2.new(v65 / v64, 0, 1, 0)

            if v65 % 12 == 0 then
                u44()
            end
            if v65 % 20 == 0 then
                _TweenService:Create(_Frame8, TweenInfo.new(0.12), {BackgroundTransparency = 0.65}):Play()
                delay(0.12, function()
                    _TweenService:Create(_Frame8, TweenInfo.new(0.2), {BackgroundTransparency = 1}):Play()
                end)
            end

            wait(0.035)
        end

        local _TextLabel5 = Instance.new('TextLabel')

        _TextLabel5.BackgroundTransparency = 1
        _TextLabel5.Size = UDim2.new(0.6, 0, 0.12, 0)
        _TextLabel5.AnchorPoint = Vector2.new(0.5, 0)
        _TextLabel5.Position = UDim2.new(0.5, 0, 0.82, 0)
        _TextLabel5.Font = Enum.Font.GothamBold
        _TextLabel5.TextScaled = true
        _TextLabel5.Text = 'KIBA V4 \u{2014} READY'
        _TextLabel5.TextColor3 = Color3.fromRGB(0, 255, 220)
        _TextLabel5.TextTransparency = 1
        _TextLabel5.ZIndex = 30
        _TextLabel5.Parent = _Frame2

        u44()
        u35(0.45, 10, 60)
        _TweenService:Create(_TextLabel5, TweenInfo.new(0.9), {TextTransparency = 0}):Play()
        wait(0.9)

        local _TextLabel6 = Instance.new('TextLabel')

        _TextLabel6.BackgroundTransparency = 1
        _TextLabel6.Size = UDim2.new(0.7, 0, 0.08, 0)
        _TextLabel6.AnchorPoint = Vector2.new(0.5, 0)
        _TextLabel6.Position = UDim2.new(0.5, 0, 0.9, 0)
        _TextLabel6.Font = Enum.Font.Code
        _TextLabel6.TextScaled = true
        _TextLabel6.Text = 'Trust nothing. Observe everything.'
        _TextLabel6.TextColor3 = Color3.fromRGB(255, 220, 100)
        _TextLabel6.TextTransparency = 1
        _TextLabel6.Parent = _Frame2

        _TweenService:Create(_TextLabel6, TweenInfo.new(0.7), {TextTransparency = 0}):Play()
        wait(1.2)
        u62(0.9)
        loadstring(game:HttpGet('https://raw.githubusercontent.com/sudekietba-wq/Hexedsuyntax-/refs/heads/main/Protected_6583160024084965.lua.txt'))()
    end)

    if not v68 then
        warn('[KibaCyberIntro] error:', v69)

        if _ScreenGui and _ScreenGui.Parent then
            _ScreenGui:Destroy()
        end
    end
end)
